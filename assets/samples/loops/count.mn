fn Main() {
    mut data iterations = 0
    mut data accumulated = 0

    loop 8 => i {
        iterations += 1
        accumulated += i
    }

    PrintV("iterations:  {}\n", iterations)
    PrintV("accumulated: {}\n", accumulated)
}