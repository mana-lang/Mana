// Again, surfacing a clobbering bug.

fn Main() {
    mut data a = 0
    mut data b = 0

    loop 20 {
        a += 1

        skip if a % 2 == 0
        b += 1
    }

    PrintV("a: {}\n", a)
    PrintV("b: {}", b)
}
